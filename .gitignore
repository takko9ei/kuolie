# LaTeX build products
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.xdv
*.run.xml
*.bcf

# Build products at the repo root only. Deliberately NOT "*.pdf": a PDF used
# as \photo{assets/avatar.pdf} has to stay tracked, or the project would break
# for everyone who clones it or imports it into Overleaf.
/*.pdf

# macOS
.DS_Store
